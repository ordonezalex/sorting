public class Mergesort implements Sorter {

    @Override
    public int[] sort(int[] unsorted) {

        if (unsorted.length > 1) {
            int h = (int) Math.floor(unsorted.length / 2), m = unsorted.length - h;
            int[] unsortedLeft = unsorted;
        }

        return unsorted;
    }
}
